import { NgModule } from '@angular/core';
import { RouterModule, Routes } from '@angular/router';

const routes: Routes = [];

@NgModule({
  imports: [NgModule, RouterModule.forChild(routes)],
  exports: [RouterModule]
})
export class ListainterfaceamentosRoutingModule { }
